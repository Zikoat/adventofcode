{
	"dependencies": {
		"@types/bun": "^1.3.3",
		"typescript": "^5.9.3",
		"zod": "^4.1.13",
		"@biomejs/biome": "2.3.10"
	},
	"scripts": {
		"debug": "bun --inspect-brk d12",
		"type": "tsc --noEmit --watch",
		"test": "bun --watch test",
		"format": "biome check --write",
		"validate": "tsc --noEmit && bun test && bun run format",
		"bigboy": "bun run validate && bun run d12-bigboy"
	}
}
